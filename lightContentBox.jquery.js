(function($){
    $.gw2tooltip = function(selector, settings){
        if(settings){
            $.extend(true, config, settings);
        }
        console.log(this);
        //vars
        return this;
    }
});